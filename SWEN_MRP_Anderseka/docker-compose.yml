version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: mymedialist
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - ./dbdata:/var/lib/postgresql/data
      - ./pgsetup.sql:/docker-entrypoint-initdb.d/pgsetup.sql